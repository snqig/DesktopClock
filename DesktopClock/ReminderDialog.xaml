<Window x:Class="DesktopClock.ReminderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提醒" Height="360" Width="400"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ResizeMode="NoResize" Background="#F5F5F7" Foreground="#1D1D1F"
        FontFamily="PingFang SC, Microsoft YaHei, sans-serif">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#1D1D1F" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FFF" />
            <Setter Property="BorderBrush" Value="#D2D2D7" />
            <Setter Property="Foreground" Value="#1D1D1F" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#1D1D1F" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#FFF" />
            <Setter Property="BorderBrush" Value="#D2D2D7" />
            <Setter Property="Foreground" Value="#1D1D1F" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="#FFF" />
            <Setter Property="BorderBrush" Value="#D2D2D7" />
            <Setter Property="Foreground" Value="#1D1D1F" />
            <Setter Property="FontSize" Value="13" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#007AFF" />
            <Setter Property="Foreground" Value="#FFF" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="24,0" />
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FFF" />
            <Setter Property="Foreground" Value="#007AFF" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="BorderBrush" Value="#D2D2D7" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="24,0" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="标题" />
            <TextBox x:Name="TitleBox" Grid.Column="1" Text="" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="备注" />
            <TextBox x:Name="DescBox" Grid.Column="1" Text="" />
        </Grid>

        <CheckBox x:Name="RecurringCheck" Grid.Row="2" Content="重复提醒" Margin="0,0,0,10"
                  Checked="RecurringCheck_Changed" Unchecked="RecurringCheck_Changed" />

        <Border x:Name="OncePanel" Grid.Row="3" Margin="20,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="日期时间" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <DatePicker x:Name="ReminderDatePicker" Width="160" />
                    <TextBox x:Name="ReminderTimeBox" Text="09:00" Width="70" Margin="8,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Border x:Name="RecurringPanel" Grid.Row="4" Margin="20,0,0,12" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="重复" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ComboBox x:Name="DayOfWeekCombo" Width="120">
                        <ComboBoxItem Content="周一" Tag="Monday" />
                        <ComboBoxItem Content="周二" Tag="Tuesday" />
                        <ComboBoxItem Content="周三" Tag="Wednesday" />
                        <ComboBoxItem Content="周四" Tag="Thursday" />
                        <ComboBoxItem Content="周五" Tag="Friday" />
                        <ComboBoxItem Content="周六" Tag="Saturday" />
                        <ComboBoxItem Content="周日" Tag="Sunday" />
                    </ComboBox>
                    <TextBox x:Name="RecurringTimeBox" Text="09:00" Width="70" Margin="8,0,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Style="{StaticResource PrimaryButton}"
                    Click="OkButton_Click" />
            <Button Content="取消" Style="{StaticResource SecondaryButton}"
                    Margin="10,0,0,0" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
