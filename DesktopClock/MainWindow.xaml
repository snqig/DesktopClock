<Window x:Class="DesktopClock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DesktopClock"
        Height="120" Width="500"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border x:Name="MainBorder" CornerRadius="10" Background="#88000000"
            BorderBrush="Transparent" BorderThickness="0">
        <Grid>
            <Grid x:Name="MainContainer">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
            </Grid>
            <Rectangle x:Name="GrayscaleOverlay" Fill="Gray" IsHitTestVisible="False" Visibility="Collapsed"/>
            <Rectangle x:Name="ColorTempOverlay" IsHitTestVisible="False" Visibility="Collapsed"/>
        </Grid>
    </Border>
</Window>
